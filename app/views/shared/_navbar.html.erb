<div class="navbar navbar-expand-sm navbar-light navbar-lewagon fixed-top">
  <%= link_to root_path, class: "navbar-brand" do %>
    <%= image_tag "minha_feira_navbar.png" %>
    <% end %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

   <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
         <p class = "btn-add-shop2"><%= link_to "Quer adicionar a sua loja?", new_shop_path%></p>
      </li>
      <li>
      <div class="d-flex justify-content-center" style="margin: 8px">
        <% if current_user %>
          <% if Cart.where(user_id: current_user.id).last %>
            <%= link_to cart_path(Cart.where(user_id: current_user.id).last.id), class: "cart-brand" do %>
             <%= image_tag "Shopping_cart_icon.svg.png", width: 40 %>
            <% end %>
          <% end %>
        <% end %>
      </div>
      </li>


      <% if user_signed_in? %>
          <li class="nav-item dropdown">
          <% if current_user.photo.attached? %>
            <%= cl_image_tag current_user.photo.key, class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false, width: 150, height: 150, crop: :thumb, gravity: :face %>
          <% else %>
            <%= image_tag "https://i.pinimg.com/originals/fd/0c/55/fd0c559856ca991e9e28937dc802f0b0.png", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false, width: 150, height: 150, crop: :thumb, gravity: :face %>
          <% end %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%= link_to "Editar usuário",  edit_user_registration_path, class: "dropdown-item" %>
            <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
         </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Login", new_user_session_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</div>



